{
  "name": "build-worker",
  "version": "0.1.0",
  "private": true,
  "description": "weWatch · Build worker — dispatches Garmin SDK compile jobs",
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO: tsx watch src/index.ts'",
    "build": "tsc -p tsconfig.json",
    "test": "echo 'no tests yet'",
    "lint": "eslint src --ext .ts || true",
    "typecheck": "tsc --noEmit || true",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@wewatch/ir-schema": "workspace:*",
    "@wewatch/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
